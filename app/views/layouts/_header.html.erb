<div class="container-fluid sticky-top">
  <nav class="nav navbar-expand-md navbar-light">
    <button
      class="navbar-toggler me-auto"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#toggleMenu"
      aria-controls="toggleMenu"
      aria-expanded="false"
      aria-label="Toggle Navigation"
    >
    <span class="navbar-toggler-icon"></span>
    </button>

    <a href="" class="navbar-brand">Journal App</a>
    <%= link_to 'Task +', new_task_path, class: "nav-link" %>
    <%= link_to 'Category +', new_category_path, class: "nav-link" %>
    
    <div class="collapse navbar-collapse" id="toggleMenu">
      <ul class="navbar-nav ms-auto">
        <li>
          <%= link_to "Home", "/", class: "nav-link" %>
        </li>
        <li>
          <%= link_to 'Categories', categories_path, class: "nav-link" %>
        </li>
        <li>
          <%= link_to 'Tasks', tasks_path, class: "nav-link" %>
        </li>
      </ul>
    </div>

    <div class="collapse navbar-collapse" id="toggleMenu">
      <ul class="navbar-nav ms-auto">
        <li>
          <a href="" class="nav-link" href="#">
            <% if user_signed_in? %>
              <%= current_user.first_name + current_user.last_name %>
            <% end %>
          </a>
        </li>
        <li>
          <% if user_signed_in? %>
            <%= link_to "Signout", destroy_user_session_path, class: "nav-link", method: :delete %>
          <% else %>
            <%= link_to "Signin", new_user_session_path, class: "nav-link" %>
          <% end %>
        </li>
      </ul>
    </div>
  </nav>
</div>