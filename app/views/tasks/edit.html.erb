<div class="mt-5 mb-5 d-flex flex-column flex-md-row mt-5">
  <div class="container d-flex justify-content-center">
    <div class="col col-md-8">
      <h1 class="p-3 mb-3">Editing Task</h1>
      <%= render 'errors' %>
      <%= form_with scope: :task, url: update_task_path, method: :patch, locale: true do |form| %>
        <%= render 'form', f: form %>
      <% end %>
    </div>
  </div>

  <div class="container">
    <h1>Editing Task</h1>
  </div>
</div>