<!-- Button trigger modal -->

<i class="bi bi-trash-fill p-2 icons__color" data-bs-toggle="modal" data-bs-target="#task<%=task.id%>Center" data-toggle="tooltip" data-placement="top" title="Delete task"></i>

<!-- Modal -->
<div class="modal fade" id="task<%=task.id%>Center" tabindex="-1" role="dialog" aria-labelledby="task<%=task.id%>CenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title text-danger" id="task<%=task.id%>LongTitle">Deleting task: <%= task.name %></h5>
        <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Are you sure you want to delete this task?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <%= link_to category_task_path(task.category_id, task),
          method: :delete do
        %>
          <button type="button" class="btn btn-primary">Delete</button>
        <% end %>
      </div>
    </div>
  </div>
</div>